import { component$ } from "@builder.io/qwik";
import Menu from "~/components/menu"



export default component$((props)=>{
    
    return (
        <dev>
            <Menu></Menu>
        </dev>
    
    )
});

